# Workflows

These workflow documents are pre-campaign task sketches retained for reference.
They have **not** been checked against the current codebase or the refactored layout.

In particular, `onboard-repo.md` is superseded by `AGENTS.md`.
For any active campaign work, follow `AGENTS.md` and the relevant task files under `docs/campaigns/`.
